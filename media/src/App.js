import './App.css';
import Player from './Player';
function App() {
  return (
    <div className="App" style={{backgroundImage:`url(${music1})`}}>
     <Player/>
    </div>
  );
}

export default App;
